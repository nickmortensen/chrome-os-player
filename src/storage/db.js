const fileMetadata = {
  put(entry) {
    console.log('to be implemented', entry);
  }
};

const watchlist = {
  put(entry) {
    console.log('to be implemented', entry);
  }
};

module.exports = {
  fileMetadata,
  watchlist
}
